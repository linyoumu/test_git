# Git 提交合并自动化脚本 v2.1

> 智能合并多个 Git 提交，支持交互式冲突解决和自定义提交信息

## 目录

- [功能特性](#功能特性)
- [快速开始](#快速开始)
- [详细说明](#详细说明)
- [使用场景](#使用场景)
- [最佳实践](#最佳实践)
- [常见问题](#常见问题)

---

## 功能特性

### ✨ 核心功能

- 🔄 **自动合并提交** - 将多个提交自动合并为一个
- 💬 **交互式提交信息** - 支持在执行前自定义提交信息
- 🛠️ **智能冲突处理** - 自动检测并等待用户解决冲突
- 🔍 **环境自适应** - 自动识别交互式/非交互式环境
- 🚀 **一键推送** - 合并后自动推送到远程仓库
- 📝 **详细日志** - 记录所有操作过程

### 🎯 v2.1 新特性

✅ **交互式提交信息输入**
✅ **Conventional Commits 格式提示**
✅ **智能冲突轮询检测**
✅ **自动跳过编辑器**
✅ **超时保护机制**

---

## 快速开始

### 基本用法

```bash
# 合并最近 3 个提交（默认）
./merge_commits.sh

# 合并最近 5 个提交
./merge_commits.sh -c 5

# 合并并指定提交信息
./merge_commits.sh -c 3 -m "feat: 整合功能模块"
```

### 查看帮助

```bash
./merge_commits.sh --help
```

---

## 详细说明

### 命令行参数

| 参数 | 简写 | 说明 | 默认值 |
|------|------|------|--------|
| `--help` | `-h` | 显示帮助信息 | - |
| `--dry-run` | `-d` | 干运行模式（仅显示操作） | - |
| `--count N` | `-c` | 要合并的提交数量 | 3 |
| `--branch NAME` | `-b` | 目标分支名称 | main |
| `--message MSG` | `-m` | 合并后的提交信息 | "合并提交：整理代码历史" |
| `--force` | `-f` | 跳过所有确认提示 | - |
| `--verbose` | `-v` | 详细输出模式 | - |
| `--auto-resolve STRATEGY` | - | 自动解决冲突（ours/theirs） | - |
| `--create-config` | - | 创建配置文件示例 | - |

**参数示例：**

```bash
# 合并 5 个提交到 dev 分支
./merge_commits.sh -c 5 -b dev

# 使用自定义提交信息
./merge_commits.sh -c 3 -m "feat: 整合最近的功能开发"

# 干运行查看效果
./merge_commits.sh -d -c 5

# 强制执行（跳过所有确认）
./merge_commits.sh -c 3 -m "合并提交" -f
```

### 执行流程

```
┌─────────────────────────────────────┐
│  1. 预检查                          │
│  • 检查是否在 Git 仓库              │
│  • 检查当前分支                     │
│  • 检查提交数量                     │
│  • 检查远程分支状态                 │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  2. 显示将要合并的提交              │
│  • 列出最近 N 个提交                │
│  • 显示提交哈希和信息               │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  3. 确认执行                        │
│  • 警告：将重写 Git 历史            │
│  • 确认继续 (y/N)                   │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  4. 📝 交互式提交信息输入 (v2.1)   │
│  • 显示当前默认提交信息             │
│  • 询问是否自定义                   │
│  • 显示 Conventional Commits 格式   │
│  • 接受用户输入                     │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  5. 储藏未提交的更改（如有）        │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  6. 执行 Rebase 合并                │
│  • 使用 squash 合并提交             │
│  • 自动跳过编辑器                   │
└─────────────┬───────────────────────┘
              ↓
         遇到冲突？
         ↓(是)    ↓(否)
    ┌──────┐      └──→ 继续
    ↓
┌─────────────────────────────────────┐
│  🛠️ 智能冲突处理                    │
│  • 列出冲突文件                     │
│  • 显示解决步骤                     │
│  • 交互式：等待按键                 │
│  • 非交互式：轮询检测（5分钟）      │
│  • 验证冲突已解决                   │
│  • 自动 git add                     │
│  • 继续 rebase                      │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  7. 修改提交信息                    │
│  • 使用用户指定的提交信息           │
│  • git commit --amend               │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  8. 推送到远程                      │
│  • git push --force-with-lease      │
│  • 安全的强制推送                   │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  9. 恢复储藏（如有）                │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  ✅ 完成                            │
│  • 显示成功信息                     │
│  • 记录日志                         │
└─────────────────────────────────────┘
```

### 冲突处理

#### 自动检测

脚本会自动检测冲突文件并显示：

```
❌ Rebase 遇到冲突

❌ 检测到以下文件存在冲突：
  ⚠️  README.md
  ⚠️  src/config.js

ℹ️  请解决上述冲突后继续...
```

#### 解决步骤

1. **编辑冲突文件**
   ```bash
   # 打开冲突文件，找到冲突标记
   <<<<<<< HEAD
   本地版本的内容
   =======
   远程版本的内容
   >>>>>>> commit-hash
   
   # 删除冲突标记，保留需要的内容
   ```

2. **保存文件**

3. **继续脚本**
   - **交互式环境**：按任意键继续
   - **非交互式环境**：脚本会自动检测（每5秒检查一次）

#### 智能环境识别

| 环境类型 | 检测方式 | 处理方式 |
|---------|---------|---------|
| 交互式终端 | `[ -t 0 ]` | 等待用户按键 |
| 非交互式/后台 | `! [ -t 0 ]` | 轮询检测（5分钟超时） |

---

## 使用场景

### 场景 1：整理开发历史

**问题**：开发过程中产生了多个细碎的提交

```bash
a1b2c3d feat: 添加登录功能
b2c3d4e fix: 修复登录按钮样式
c3d4e5f fix: 修复登录验证逻辑
d4e5f6g docs: 更新登录文档
```

**解决**：使用脚本合并为一个清晰的提交

```bash
./merge_commits.sh -c 4 -m "feat: 完成用户登录功能"
```

**结果**：
```bash
e5f6g7h feat: 完成用户登录功能
```

### 场景 2：提交前整理

**场景**：准备提交 PR，但提交历史混乱

```bash
# 查看最近的提交
git log --oneline -5

# 干运行查看效果
./merge_commits.sh -d -c 3

# 确认无误后执行
./merge_commits.sh -c 3
```

### 场景 3：修复错误提交

**场景**：发现最近几个提交有问题，需要重新整理

```bash
# 合并最近3个提交并重新写提交信息
./merge_commits.sh -c 3
# 在交互界面输入正确的提交信息
```

### 场景 4：多次小修改合并

**场景**：review 后多次小修改，需要合并

```bash
# 查看提交
git log --oneline -n 5

# 合并最近5个提交
./merge_commits.sh -c 5 -m "refactor: 根据 review 意见优化代码"
```

---

## 最佳实践

### 📋 提交信息规范

推荐使用 **Conventional Commits** 格式：

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### Type 类型

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat: 添加用户登录功能` |
| `fix` | 修复 Bug | `fix: 修复数据加载失败问题` |
| `refactor` | 重构代码 | `refactor: 优化用户模块结构` |
| `perf` | 性能优化 | `perf: 优化列表查询性能` |
| `style` | 代码格式 | `style: 统一代码缩进格式` |
| `docs` | 文档更新 | `docs: 更新 API 使用文档` |
| `test` | 测试相关 | `test: 添加用户模块单元测试` |
| `chore` | 构建/工具 | `chore: 更新依赖包版本` |
| `build` | 构建系统 | `build: 优化 webpack 配置` |
| `ci` | CI/CD | `ci: 添加自动部署脚本` |

#### 示例

```bash
# ✅ 好的提交信息
feat(auth): 添加 OAuth 2.0 登录支持
fix(api): 修复用户数据返回格式错误
refactor: 重构用户模块，提升代码可维护性
perf(query): 优化数据库查询，减少 50% 响应时间

# ❌ 不好的提交信息
修改代码
fix bug
更新
完成功能
```

### ⚠️ 使用注意事项

1. **仅用于个人分支**
   - ❌ 不要在共享分支（main, master, dev）上使用
   - ✅ 只在自己的 feature 分支上使用

2. **确认无其他人使用该分支**
   - 强制推送会覆盖远程历史
   - 可能导致其他人的工作丢失

3. **合并前先备份**
   ```bash
   # 创建备份分支
   git branch backup-$(date +%Y%m%d-%H%M%S)
   
   # 执行合并
   ./merge_commits.sh -c 3
   ```

4. **小范围合并**
   - 建议一次合并 3-5 个提交
   - 避免合并过多提交导致冲突

5. **使用干运行模式**
   ```bash
   # 先查看效果
   ./merge_commits.sh -d -c 5
   
   # 确认无误后执行
   ./merge_commits.sh -c 5
   ```

### 🛡️ 安全建议

```bash
# 1. 执行前查看将要合并的提交
git log --oneline -n 5

# 2. 使用干运行模式预览
./merge_commits.sh -d -c 3

# 3. 确保工作区干净
git status

# 4. 创建备份分支
git branch backup-feature

# 5. 执行合并
./merge_commits.sh -c 3

# 6. 如果出错，可以回滚
git reset --hard backup-feature
```

---

## 常见问题

### Q1: 脚本执行后卡住不动？

**原因**：可能在等待用户输入或解决冲突

**解决**：
- 检查是否有冲突需要解决
- 查看终端是否在等待输入
- 检查进程：`ps aux | grep merge_commits`
- 终止进程：`pkill -f merge_commits.sh`

### Q2: 遇到 Vim 交换文件警告？

```
Swap file ".COMMIT_EDITMSG.swp" already exists!
```

**解决**：
```bash
# 删除交换文件
rm .git/.COMMIT_EDITMSG.sw*

# 中止当前 rebase
git rebase --abort

# 重新执行脚本
./merge_commits.sh -c 3
```

### Q3: 如何撤销脚本的操作？

**在推送前**：
```bash
# 查看 reflog
git reflog

# 回滚到之前的状态
git reset --hard HEAD@{1}
```

**已经推送**：
```bash
# 强制回滚（谨慎使用）
git reset --hard <原始commit-hash>
git push --force
```

### Q4: 冲突太多怎么办？

**方案 1**：中止并手动处理
```bash
# 如果脚本在等待，按 Ctrl+C 或选择中止
git rebase --abort

# 手动 rebase
git rebase -i HEAD~3
```

**方案 2**：使用 --auto-resolve（未来版本）
```bash
# 自动保留本地版本
./merge_commits.sh -c 3 --auto-resolve ours

# 自动保留远程版本
./merge_commits.sh -c 3 --auto-resolve theirs
```

### Q5: 如何配置默认参数？

创建配置文件：
```bash
./merge_commits.sh --create-config
```

编辑 `.git-squash.conf`：
```bash
# Git Squash 配置文件
COMMIT_MSG="feat: 整合功能开发"
BRANCH_NAME="dev"
COMMIT_COUNT=3
LOG_FILE="./git-squash.log"
```

### Q6: 脚本支持哪些操作系统？

| 系统 | 支持状态 | 说明 |
|------|---------|------|
| macOS | ✅ 完全支持 | 已测试 |
| Linux | ✅ 完全支持 | 已测试 |
| Windows (Git Bash) | ✅ 支持 | 需要 Git Bash |
| Windows (WSL) | ✅ 支持 | - |

### Q7: 合并后发现错误如何处理？

```bash
# 方法 1：Amend 修改
git commit --amend -m "新的提交信息"
git push --force

# 方法 2：Reset 回滚
git reset --hard <之前的commit>
git push --force

# 方法 3：使用备份分支
git reset --hard backup-branch
git push --force
```

### Q8: 脚本日志在哪里？

```bash
# 查看日志文件
cat git-squash.log

# 实时查看
tail -f git-squash.log

# 查看最近20行
tail -20 git-squash.log
```

---

## 版本历史

### v2.1 (2026-04-28)

**新增功能**：
- ✨ 交互式提交信息输入
- 💡 Conventional Commits 格式提示
- 🔄 智能冲突轮询检测
- 🚫 自动跳过 Vim 编辑器
- ⏱️ 超时保护机制（5分钟）
- 🎯 环境自适应（交互式/非交互式）

**改进**：
- 优化用户交互体验
- 增强错误处理
- 完善日志记录

### v2.0 (2026-04-27)

**新增功能**：
- 🛠️ 智能冲突处理
- 📊 详细日志记录
- 🎨 彩色输出
- ⚙️ 配置文件支持

---

## 许可证

MIT License

---

## 贡献

欢迎提交 Issue 和 Pull Request！

---

## 联系方式

如有问题，请联系：[TBD]

---

**⚠️ 重要提醒：此脚本会重写 Git 历史，仅在个人分支上使用！**

